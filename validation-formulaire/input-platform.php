<?php
if (!empty($plateform_clear)) {
    // if (in_array("Switch", $plateforms_clear) || in_array("PS5", $plateforms_clear) || in_array("PS4", $plateforms_clear) || in_array("PC", $plateforms_clear)|| in_array("Playstation 2", $plateforms_clear)|| in_array("Gameboy Advance", $plateforms_clear)|| in_array("Playstation", $plateforms_clear)) {
    // } else {
    //     $error["error"] = "<span class=text-red-500>Valeurs non existantes !</span>";
    // }
} else {
    $error["genre"] = $errorMessage;
}
?>