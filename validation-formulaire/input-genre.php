<?php
//on vérifie que le user
if (!empty($genre_clear)) {
    // if(in_array("RPG", $genre_clear) || in_array("Aventure", $genre_clear) || in_array("RogueLike", $genre_clear) || in_array("Infiltration", $genre_clear)){

    // }else{
    //     $error["error"] ="<span class=text-red-500>Valeurs non existantes !</span>";
    // } 
}else{
    // $error["genre"] = $errorMessage;
}

?>